import React from 'react'

const DeleteButton = ({calcText, setCalcText}) => {
  return (
    <button id='deleteBtn' className='uppercase delete-btn btn'>
      del
    </button>
  )
  
}

export default DeleteButton